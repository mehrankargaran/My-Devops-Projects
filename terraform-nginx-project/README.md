# Terraform Nginx Project

This project is for DevOps Exercise #20 and includes **two parts for installing Nginx**:

1. Installing Nginx on the **local VM**  
2. Installing Nginx on **two remote VMs**

---

## 🟢 Part 1: Nginx on Local VM

- Files location: `local/`  
- VM IP: 192.168.2.118  
- User: mehran  
- Password: amd233  
- Method: Terraform + local-exec  

### Running Terraform:

#```bash
cd local
terraform init
terraform apply -auto-approve

Verifying installation:
------------------------
systemctl status nginx | grep Active
curl http://localhost:8080


The custom Kargaran HTML page should be displayed.

## 🟢 Part 2: Nginx on Remote Vs


Files location: remote/

VM IPs: 192.168.2.119 and 192.168.2.120

User: mehran

Password: amd233

Method: Terraform + remote-exec + SSH

Running Terraform:
cd remote
terraform init
terraform apply -auto-approve

Verifying installation:
ssh mehran@192.168.2.119
systemctl status nginx | grep Active
curl http://192.168.2.119:8080
exit

ssh mehran@192.168.2.120
systemctl status nginx | grep Active
curl http://192.168.2.120:8080
exit


The custom HTML page should be displayed on both VMs.

⚠️ Important Notes

The terraform.tfstate files and .terraform/ folder are not uploaded to GitHub.

The install_nginx.sh script in the local/ folder is generated by Terraform, while the one in remote/ is manually created.

Nginx port on all VMs: 8080
